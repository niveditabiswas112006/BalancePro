# Screenshots Directory

Save your academic screenshots here. Recommended screenshots for your project report:

1.  `01_dashboard_active.png`: The main dashboard page showing stable routing in progress.
2.  `02_simulate_failure.png`: A view demonstrating Server-1 marked as crashed with the failover routing logs in the terminal window.
3.  `03_server_recovery.png`: A view showing the automatic recovery log after hitting the "Recover Server" button on a previously crashed node.
4.  `04_metrics_and_charts.png`: Chart.js visualizations representing latency and connection loads.
5.  `05_thunder_client_test.png`: A screenshot of a POST request to `/request` through VS Code's Thunder Client.
